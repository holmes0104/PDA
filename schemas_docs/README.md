# JSON Schema documentation

Generated from Pydantic models. To regenerate:

```bash
python -c "from pda.schemas.export_schemas import export_json_schemas; export_json_schemas('schemas_docs')"
```

From project root with `pip install -e .` or `PYTHONPATH=.`.
