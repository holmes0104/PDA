# PDA — LLM Product Discoverability Auditor
# Copy to .env and fill in your keys.  Never commit .env!

# ──────────────────────────────────────────────
# LLM Provider
# ──────────────────────────────────────────────
PDA_LLM_PROVIDER=openai          # openai | anthropic

# OpenAI (required if PDA_LLM_PROVIDER=openai)
OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic (required if PDA_LLM_PROVIDER=anthropic)
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: override model names
# PDA_OPENAI_MODEL=gpt-4o
# PDA_ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# ──────────────────────────────────────────────
# Embeddings
# ──────────────────────────────────────────────
PDA_EMBEDDING_MODEL=openai       # openai | sentence-transformers

# ──────────────────────────────────────────────
# Data & Storage
# ──────────────────────────────────────────────
# All runtime data (uploads, chroma, projects) lives here.
# Local dev: ./data      Render (persistent disk): /data
PDA_DATA_DIR=./data

# ──────────────────────────────────────────────
# Server (backend)
# ──────────────────────────────────────────────
PORT=8000

# CORS — comma-separated list of allowed origins.
# Set to your Vercel frontend URL in production.
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Max PDF upload size in bytes (default 50 MB)
# MAX_UPLOAD_BYTES=52428800

# ──────────────────────────────────────────────
# Frontend (set in Vercel dashboard, not here)
# ──────────────────────────────────────────────
# NEXT_PUBLIC_API_BASE_URL=https://your-backend.onrender.com
