# PDA — LLM Product Discoverability Auditor
# Designed for Vaisala by Thanh Nguyen (Holmes)
# Copy to .env and fill in your keys.  Never commit .env!
#
# ──────────────────────────────────────────────
# Authentication (built-in credentials)
# ──────────────────────────────────────────────
# Credential 1:  admin    / Vaisala2026!
# Credential 2:  tnguyen  / PDA@Holmes
# (Credentials are hardcoded in backend/auth.py — edit there to change.)

# ──────────────────────────────────────────────
# LLM Provider
# ──────────────────────────────────────────────
PDA_LLM_PROVIDER=openai          # openai | anthropic

# OpenAI (required if PDA_LLM_PROVIDER=openai)
OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic (required if PDA_LLM_PROVIDER=anthropic)
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: override model names (default: gpt-5.2)
# PDA_OPENAI_MODEL=gpt-5.2
# PDA_ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# ──────────────────────────────────────────────
# Embeddings
# ──────────────────────────────────────────────
PDA_EMBEDDING_MODEL=openai       # openai | sentence-transformers

# ──────────────────────────────────────────────
# Data & Storage
# ──────────────────────────────────────────────
# All runtime data (uploads, chroma, projects) lives here.
# Local dev: ./data      Render (persistent disk): /data
PDA_DATA_DIR=./data

# Vector store backend: chroma (default, local) or pgvector (Postgres)
PDA_VECTOR_BACKEND=chroma

# Database URL for pgvector backend (only used when PDA_VECTOR_BACKEND=pgvector)
# PDA_DATABASE_URL=postgresql://pda:pda_dev@localhost:5432/pda

# ──────────────────────────────────────────────
# Server (backend)
# ──────────────────────────────────────────────
PORT=8000

# CORS — comma-separated list of allowed origins.
# Set to your Vercel frontend URL in production.
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional: allow all Vercel deployments (production + previews)
# CORS_ORIGIN_REGEX=https://.*\.vercel\.app

# Max PDF upload size in bytes (default 50 MB)
# MAX_UPLOAD_BYTES=52428800

# ──────────────────────────────────────────────
# Frontend (set in Vercel dashboard, not here)
# ──────────────────────────────────────────────
# NEXT_PUBLIC_API_BASE_URL=https://your-backend.onrender.com
